

export default function ShadowEffect({ children }) {
    return (
        <div className="jm-logo">
            <a href="#">
            <span> </span>
            {children}
            </a> 
            </div>
    )
}